[pytest]
testpaths = tests
pythonpath = src scripts
addopts = --strict-markers
# FastAPI 0.141 emits this compatibility warning with the pinned httpx release.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated:DeprecationWarning
